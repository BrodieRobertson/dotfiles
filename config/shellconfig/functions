# cd when exiting lf
lc () {
    tmp=$(mktemp)
    lf -last-dir-path="$tmp" "$@"
    if [ -f "$tmp" ]; then
        dir=$(< "$tmp")
        rm -f "$tmp"
        [ -d "$dir" ] && [ "$dir" != "$(pwd)" ] && cd "$dir"
    fi
}

helloworld() {
    echo "HelloWorld"
}
